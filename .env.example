# Public deployment URL used for canonical links, social metadata, sitemap and robots.txt.
# Leave unset to use the default Render URL.
VITE_SITE_URL=https://murad-muse-gemeni.onrender.com

# Firebase client configuration is maintained in firebase-applet-config.json.
# Do not duplicate those values here; Vite loads the checked-in public Firebase config.
